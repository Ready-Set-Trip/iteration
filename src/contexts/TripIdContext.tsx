// import { createContext } from "react";

// export const UserContext = createContext(null);

//context for trip, change the name
import React from 'react';
const TripIdContext = React.createContext({});
export default TripIdContext;
